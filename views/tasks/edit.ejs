<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Task | APEX</title>
    <link rel="stylesheet" href="/css/style.css">

</head>
<body>
    <nav class="navbar">
        <div class="container nav-content">
            <a href="/tasks" class="logo">APEX</a>
            <div class="nav-links">
                <span style="color: var(--secondary);">Welcome, <%= user.username %></span>
                <a href="/auth/logout" class="btn btn-secondery">Logout</a>
            </div>
        </div> 
    </nav>

    <div class="container">
        <div class="from-contsiner">
            <div class="from-header">
                <h1>Edit Task</h1>
        </div>
        <form action="/tasks/<%=todo._id %>?_method=PUT" method="POST">
            <div class="from-group">
                <label for="title">Task Title</label>
                <input type="text" id="title" name="title" class="from-control" value="<%= todo.title%>" required>
                <div class="from-group">
                    <label for="description">Description</label>
                    <textarea id="description" name="description" class="form-control"><%= todo.description || '' %></textarea>
                </div>
                <div class="from-group">
                    <label for="dueDate">Due Date</label>
                    <input type="date" id="dueDate" name="dueDate" class="form-control"
                    value="<%= todo.dueDate ? todo.dueDate.toISOString().split('T')[0] : ''%>">
                </div>
                <div class="form-group">
                    <label for="priority">Priority</label>
                    <select id="priority" name="priority" class="select-control">
                        <option value="High"<%=todo.priority==='High' ?'selected' : ''%>>High</option>
                        <option value="Medium" <%=todo.priority === 'Medium' ? 'select' :%>>Low</option>

                    </select>
                </div>
                <div class="form-actions">
                    <a href="/task" class="btn btn-secondery">Cancel</a>
                    <button type="submit" class="btn btn-primary">Update Task</button>
                </div>
            
        </form>
    </div>
</div>
</body>
</html>
